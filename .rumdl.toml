[global]
# MD013 (line length) is disabled because the README and CHANGELOG contain
# intentionally long lines: table cells with rule descriptions, code blocks,
# and inline examples. Same rationale as disabling line-length in .yamllint.yaml.
disable = ["MD013"]
